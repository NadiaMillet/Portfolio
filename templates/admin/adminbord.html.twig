{% extends '/main/navfoot.html.twig' %}

{% block navbar %}
	{{parent()}}
	<h3 class="text-center mt-5">
		Hello Nadia !</h3>
	<div class="border border-secondary p-5 mb-5 affichage-admin">
		<p class="text-xl-center">
			Bonjour Admin, accédez à vos éspaces directement depuis le liens ci-dessous !
		</p>
		<button type="button" class="btn btn-secondary btn-retour">
			<a href="{{path("show_profil")}}" style="color:white;">Modifier ton profil</a>
		</button>

	{% endblock %}
